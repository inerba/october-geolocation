# ===================================
#  Field Definitions
# ===================================

tabs:
    fields:

        # Google Maps API Key
        google_maps_key:
            label: inerba.geolocation::lang.settings.google_maps_key
            comment: inerba.geolocation::lang.settings.google_maps_key_comment
            tab: inerba.geolocation::lang.settings.credentials_tab
